# V1.2 Release Gates Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Extend the local and CI release matrix so V1.2 auth-profile runtime, provider expansion, and provider-aware UI behavior are all covered by repeatable release gates.

**Architecture:** V1.2 should preserve the existing V1 and V1.1 release matrix, then add focused suites for the new provider/auth/profile behavior without broadening the workflow into platform areas outside the desktop runtime.

**Tech Stack:** GitHub Actions (Windows), Bun, Rust/Cargo, Vitest, existing release workflow and test suites.

---

## File Structure

### CI

- Modify: `.github/workflows/windows-release-gates.yml`

### Verification

- Verify: `src-tauri/tests/auth_profile_runtime.rs`
- Verify: `src-tauri/tests/provider_registry_v12.rs`
- Verify: `src-tauri/tests/provider_inventory_v12.rs`
- Verify: `src/test/app-shell.test.tsx`
- Verify: `src/test/tauri.test.ts`

---

## Task 1: Add V1.2 Suite Expectations To The Release Workflow

**Files:**
- Modify: `.github/workflows/windows-release-gates.yml`

- [ ] **Step 1: Write the failing workflow-content expectation**

Expected additions:

```text
cargo test --manifest-path src-tauri/Cargo.toml --test auth_profile_runtime
cargo test --manifest-path src-tauri/Cargo.toml --test provider_registry_v12
cargo test --manifest-path src-tauri/Cargo.toml --test provider_inventory_v12
```

- [ ] **Step 2: Verify the workflow is missing the V1.2 suites**

Run:

```bash
rg -n "auth_profile_runtime|provider_registry_v12|provider_inventory_v12" .github/workflows/windows-release-gates.yml
```

Expected: MISS before the V1.2 gate update lands.

- [ ] **Step 3: Update the workflow with the V1.2 suite coverage**

```yaml
- run: bun install --frozen-lockfile
- run: bun run test
- run: cargo test --manifest-path src-tauri/Cargo.toml
```

- [ ] **Step 4: Verify the V1.2 gates are present**

Run:

```bash
rg -n "auth_profile_runtime|provider_registry_v12|provider_inventory_v12|bun run test|cargo test --manifest-path src-tauri/Cargo.toml" .github/workflows/windows-release-gates.yml
```

Expected: matches for the V1.2 focused suites and the existing matrix commands.

- [ ] **Step 5: Commit**

```bash
git add .github/workflows/windows-release-gates.yml
git commit -m "chore(ci): extend release gates for v1.2 suites"
```

## Task 2: Run The Full V1.2 Local Verification Matrix

**Files:**
- Verify only

- [ ] **Step 1: Run frontend tests**

Run:

```bash
bun run test
```

Expected: PASS.

- [ ] **Step 2: Run backend tests**

Run:

```bash
cargo test --manifest-path src-tauri/Cargo.toml
```

Expected: PASS.

- [ ] **Step 3: Run focused V1.2 suites**

Run:

```bash
cargo test --manifest-path src-tauri/Cargo.toml --test auth_profile_runtime --test provider_registry_v12 --test provider_inventory_v12 -- --nocapture
```

Expected: PASS.

- [ ] **Step 4: Commit the verified release-gate checkpoint**

```bash
git commit --allow-empty -m "chore(v1.2): record verified local release gates"
```

- [ ] **Step 5: Push**

```bash
git pull --rebase
bd dolt push
git push
git status
```

Expected: synced branch with only intentional local-only user changes remaining.
