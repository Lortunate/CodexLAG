# V1.2 Execution Prompt

Use this prompt to execute the V1.2 implementation plan in a fresh agent session.

---

You are executing CodexLAG V1.2 implementation work.

Project root:

- `E:/Projects/Rust/CodexLAG`

Primary docs:

- `docs/superpowers/specs/foundation/codexlag-foundation.md`
- `docs/superpowers/specs/2026-04-15-v1.2-design.md`
- `docs/superpowers/plans/2026-04-15-v1.2-master-plan.md`
- `docs/superpowers/plans/2026-04-15-v1.2-auth-profile-runtime-plan.md`
- `docs/superpowers/plans/2026-04-15-v1.2-provider-expansion-plan.md`
- `docs/superpowers/plans/2026-04-15-v1.2-provider-aware-ui-plan.md`
- `docs/superpowers/plans/2026-04-15-v1.2-release-gates-plan.md`

Reference baseline:

- `E:/Projects/3rdp/agents/CLIProxyAPI`

Execution rules:

1. Use `superpowers:executing-plans` or `superpowers:subagent-driven-development`.
2. Prioritize subagents by default.
3. Prioritize parallel execution whenever file ownership is disjoint.
4. Use git worktrees for implementation tracks instead of one shared working directory.
5. Keep V1.2 constrained to the CLIProxyAPI-aligned account login modes only:
   - `browser_oauth_pkce`
   - `static_api_key`
6. Do not introduce:
   - device-code login
   - custom protocol callback login
   - remote multi-user behavior
   - hosted control plane behavior
   - plugin loading from external binaries or remote manifests

Required worktree layout:

- `.worktrees/v1_2_auth`
- `.worktrees/v1_2_registry`
- `.worktrees/v1_2_ui`
- `.worktrees/v1_2_release`

Required ownership split:

- `v1_2_auth`
  - `docs/superpowers/plans/2026-04-15-v1.2-auth-profile-runtime-plan.md`
  - `src-tauri/src/auth/`
  - `src-tauri/src/models.rs`
  - `src-tauri/src/state.rs`
  - `src-tauri/tests/auth_profile_runtime.rs`
- `v1_2_registry`
  - `docs/superpowers/plans/2026-04-15-v1.2-provider-expansion-plan.md`
  - `src-tauri/src/providers/`
  - `src-tauri/src/commands/accounts.rs`
  - `src-tauri/src/db/repositories.rs`
  - `src-tauri/tests/provider_registry_v12.rs`
  - `src-tauri/tests/provider_inventory_v12.rs`
- `v1_2_ui`
  - `docs/superpowers/plans/2026-04-15-v1.2-provider-aware-ui-plan.md`
  - `src/features/accounts/`
  - `src/features/overview/`
  - `src/features/policies/`
  - `src/lib/types.ts`
  - `src/lib/tauri.ts`
  - `src/test/app-shell.test.tsx`
  - `src/test/tauri.test.ts`
- `v1_2_release`
  - `docs/superpowers/plans/2026-04-15-v1.2-release-gates-plan.md`
  - `.github/workflows/windows-release-gates.yml`
  - final integration and verification

Execution order:

1. Read the foundation spec, V1.2 design, and V1.2 master plan.
2. Verify `.worktrees/` is ignored before creating worktrees.
3. Create the four worktrees above.
4. Dispatch four subagents in parallel, one per worktree, with strict file ownership.
5. Require each subagent to follow its subplan task-by-task.
6. Require each subagent to run focused tests before reporting completion.
7. Review and merge each worktree branch back to the integration branch only after its focused tests pass.
8. Run the V1.2 release-gates plan after all workstreams are integrated.
9. Run:

```bash
bun run test
cargo test --manifest-path src-tauri/Cargo.toml
cargo test --manifest-path src-tauri/Cargo.toml --test auth_profile_runtime --test provider_registry_v12 --test provider_inventory_v12 -- --nocapture
```

10. Only after all verification passes:

```bash
git pull --rebase
bd dolt push
git push
git status
```

Review standard:

- Prefer current `master` behavior when conflicts arise unless the V1.2 spec explicitly requires divergence.
- Preserve existing V1 and V1.1 release coverage.
- Keep auth/runtime and UI contracts synchronized.
- Do not expand scope beyond the V1.2 design.

Completion standard:

- All V1.2 subplans executed or explicitly updated
- Focused tests pass per workstream
- Full frontend and backend release gates pass
- Branches merged
- Remote pushed successfully
